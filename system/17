# Find the command that allows you to connect via ssh on the VM. (In parallel with
# the graphic session)
# first, install openssh-server using sudo apt-get install openssh-server ii
# then, start the service using sudo service ssh start
# then, get the IP address of the Debian system in VM using hostname -I i..e, 10.0.2.15
# to connect via ssh on the VM use the following commands, ssh user@IPaddress
ssh anjala@10.0.2.15